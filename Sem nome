var counter=0;
var timeleft=60;
function setup(){
  noCanvas();

  var timer = select('#timer');
  timer.html(timeleft-counter);

  function timeIt(){
    counter++
    timer.html(timeleft-counter);
  }
  setInterval(timeIt, 1000);
}
